#include "../Headers/Sprite.h"

Sprite::Sprite(std::string image, glm::vec2 position, float rotation, glm::vec2 scale, glm::vec3 color)
	: image(image), position(position), rotation(rotation), scale(scale), color(color) {

}
